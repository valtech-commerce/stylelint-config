//--------------------------------------------------------
//-- stylelint config
//--------------------------------------------------------
import loader from "@absolunet/stylelint-loader";

const config = loader(__dirname);

module.exports = config;
